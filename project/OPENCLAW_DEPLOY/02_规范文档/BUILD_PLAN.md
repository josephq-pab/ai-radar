# 对公 AI 雷达站｜建设总计划

## 1. 文档目的

本文件用于把当前阶段的项目建设拆成可执行、可验收、可持续追加的计划，不依赖会话记忆。

使用规则：
- 阶段完成后更新状态
- 新阶段开始前先补充输入、输出、验收标准
- 遇到偏离时先改计划，再改代码/页面

## 2. 当前阶段总目标

在 OpenClaw 环境内，把“对公 AI 雷达站”推进到：
- 基线稳定
- 主链路可体验
- 数据链路可重建
- 规则链路可解释
- 跟踪闭环可落地
- 后续具备迁移到可信环境的可交接性

## 3. 阶段划分

---

## 阶段 A：基线对齐与治理固化
**状态：已完成** ✅

### 目标
把前期资料正式上升为项目基线，并建立不依赖记忆检索的治理文档体系。

### 输入
- 前期资料目录 `/Users/josephq/Downloads/对公AI雷达站/对公AI雷达站/`
- 当前项目目录 `projects/ai-radar-station/`

### 输出
- `docs/PROJECT_BASELINE.md`
- `docs/BASELINE_ALIGNMENT.md`
- `docs/BUILD_PLAN.md`
- `docs/PROGRESS_LOG.md`
- 干净的 `docs/NEXT_STEPS.md`

### 验收标准
- 已明确上位文档优先级
- 已明确一级 / 二级 / 预留页面口径
- 已明确建设纪律与进展落盘规则
- 项目后续不再仅依赖会话记忆延续

### 当前重点动作
1. 固化基线
2. 建立建设计划
3. 建立进展日志
4. 修正已有文档残留与口径冲突

---

## 阶段 B：四页主链路收口
**状态：已完成** ✅

### 目标
把首页总览、存款深案例、贷款深案例、重点事项跟踪页做成正式主链路。

### 输入
- 前期资料中的原型范围与页面架构说明
- 当前已存在的 `index.html` / `deposit.html` / `loan.html` / `tracking.html`

### 输出
- 主链路四页结构稳定
- 主跳转可用
- 二级页定位清晰

### 验收标准
- 4 个核心页面可访问
- 页面间主跳转链路可用
- 跟踪页不再是占位页面
- 周报 / 同业页已降级为二级承接

### 当前重点动作
1. 保持页面层级稳定
2. 不再扩张一级页面数量
3. 检查主导航与跳转是否持续一致

---

## 阶段 C：数据中间层与 tracking 正式化
**状态：已完成** ✅

### 目标
把 tracking 页依赖的关键字段从前端推断下沉为正式中间层数据。

### 输入
- `reports/review-queue.json`
- `reports/review-status.json`
- `data/processed/recommendation_rules.json`

### 输出
- `scripts/build_tracking_items.py`
- `reports/tracking-items.json`
- 前端改为消费正式 tracking 数据对象

### 验收标准
- `layer / priority / nextAction / sourceName / sourceHref / needReview` 不再主要由前端推断
- tracking 页直接读中间层
- tracking 字段定义可复用到 API / 后端阶段

### 当前重点动作
1. 完成 `tracking-items.json` 构建
2. 重建 bundle 并验证 tracking 页
3. 统一状态口径与来源口径
4. 将 review 状态与 tracking 业务状态拆分为独立规则文件，减少脚本硬编码
5. 支持 tracking 业务状态独立记录，并允许覆盖默认 review -> tracking 映射

---

## 阶段 D：规则链路深化
**状态：已完成** ✅

### 目标
把偏好画像、解释规则、建议生成规则从“展示层存在”推进到“生成链真正消费”。

### 输入
- `preference_profile.json`
- `section_preferences.json`
- `domain_templates.json`
- `interpretation_rules.json`
- `recommendation_rules.json`

### 输出
- 周报正文更稳定地体现观察 / 策略 / 行动分层
- 案例页建议动作与规则一致
- tracking 闭环与规则一致

### 验收标准
- 页面、事项、周报三者的口径一致性明显提升
- 规则不再只是展示文本，而是进入生成链路

### 当前重点动作
1. 让周报生成真正消费建议规则
2. 让案例页的建议动作尽量由中间层驱动
3. 让 tracking 页的优先级与下一步动作尽量由中间层驱动

---

## 阶段 E：试运行与周节奏固化
**状态：已完成** ✅

### 目标
按试运行 SOP 把项目推进到可持续周运行状态。

### 输入
- 试运行 SOP
- 首周任务表
- 当周新增信息与数据

### 输出
- 可执行的周节奏
- 可回看的一周进展记录
- 更真实的 review 数据

### 验收标准
- 信息采集—双案例更新—事项入池—跟踪闭环—周报输出可持续跑通
- 页面 / 事项 / 周报一致性可检查

---

## 阶段 F：工程化迁移准备
**状态：待开始**

### 目标
为可信环境中的正式部署准备结构化交接物。

### 输出
- 更稳定的中间层 schema
- API 草图
- 前后端职责边界
- 配置与运行手册

### 验收标准
- 当前原型不是一次性 demo，而是可以拆解迁移的原型底座

## 4. 当前严格执行顺序

短期内严格按以下顺序执行，不随意跳步：

1. 基线治理文档固化
2. tracking 中间层正式化
3. 四页主链路一致性校验
4. 规则链路进一步进入生成过程
5. 试运行周节奏落地
6. 工程化迁移准备

## 5. 当前明确不做

当前阶段不优先做：
- 新增更多一级页面
- 高保真视觉大改
- 权限体系与审批流
- 复杂自动专题系统
- 全量政策 / 行业页深化

## 6. 当前下一步（紧邻执行）

1. 完成 tracking 中间层数据下沉并验证
2. 修复并重建 memory 不稳定情况下的项目治理文档机制
3. 让每轮推进同步写入 `PROGRESS_LOG.md`
